//
//  ContentView.swift
//  TalkLens
//
//  Created by Arun Mishra on 11/18/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("TalkLens App (to be built).")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
